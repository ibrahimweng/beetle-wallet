{
  "name": "beetle-wallet",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Beetle — a Nigerian bank app with an agent that thinks, built from the Figma file.",
  "scripts": {
    "start": "npx --yes http-server . -p 8080 -c-1 -o",
    "build": "node build.js",
    "test": "node build.js && node test/render.mjs && node test/interact.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
